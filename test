#!/usr/bin/env bash

set -euo pipefail

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
cd "$repo_root"

if [[ ! -f build/debug/CMakeCache.txt ]]; then
  cmake --preset debug
fi

cmake --build --preset debug
ctest --preset debug "$@"